[pytest]
minversion = 6.0
testpaths = tests
addopts = -q
markers =
    examples: run example scripts
    live_api: tests that require live API keys
    integration: longer running integration tests
    requires_env: tests that require specific env vars
